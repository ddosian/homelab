---
services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: unless-stopped
    environment:
      DOMAIN: "https://vaultwarden.dontddos.me"
      ADMIN_TOKEN: ${ADMIN_TOKEN}
    volumes:
      - /opt/containers/vaultwarden/data/:/data/
    networks:
      - newt

networks:
  newt:
    external: true