apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: homarr
  namespace: homarr
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`homarr.local.dontddos.me`)
      kind: Rule
      services:
        - name: homarr-helm
          port: 7575